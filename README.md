# Varlock Homebrew Tap

This is the [Homebrew](https://brew.sh/) [tap](https://docs.brew.sh/Taps.html) for [Varlock](https://varlock.dev).

## Usage

To install varlock this way, run:

```sh
brew install dmno-dev/tap/varlock
```
